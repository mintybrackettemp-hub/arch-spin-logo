# arch-spin-logo

## Installation

To run the installation, type this:
```bash
git clone https://github.com/mintybrackettemp-hub/arch-spin-logo
cd arch-spin-logo
sh ./install.sh
```
After you have done the setup, simply run:
```bash
archspin
```
